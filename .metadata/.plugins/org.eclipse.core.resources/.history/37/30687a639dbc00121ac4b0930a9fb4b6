import javax.swing.*;

@SuppressWarnings("serial")
public class GUI extends JFrame{
	JPanel pRate;
	JButton bRate;
	JLabel lRate;

	public GUI() {
		pRate = new JPanel();
		bRate = new JButton();
		lRate = new JLabel();
		
		pRate.add(bRate);
		pRate.add(lRate);
		this.add(pRate);
	}
}